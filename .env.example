# Copy to .env to use real Postgres. Without it, the server uses a local file store (db.json).
DATABASE_URL=postgres://user:password@localhost:5432/b2bhacks
PORT=8000
# Admin dashboard login (defaults shown; override here to rotate credentials)
ADMIN_USER=B2B hacks.prajwals
ADMIN_PASS=prajwals246_API@red24
